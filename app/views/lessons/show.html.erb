<p id="notice"><%= notice %></p>




<p>
  <strong>Lesson title:</strong>
  <%= @lesson.lesson_title %>
</p>

<h3>Explanation</h3>
<!--
<p>
  <strong>Explanation:</strong>
  <%= @lesson.explanation %>
</p>

<p>
  <strong>Prompt:</strong>
  <%= @lesson.prompt %>
</p>

<p>
  <strong>Role model:</strong>
  <%= @lesson.role_model %>
</p>

<p>
  <strong>Performance:</strong>
  <%= @lesson.performance %>
</p>

<p>
  <strong>Explanation script:</strong>
  <%= @lesson.explanation_script %>
</p>

<p>
  <strong>Prompt script:</strong>
  <%= @lesson.prompt_script %>
</p>

<p>
  <strong>Model script:</strong>
  <%= @lesson.model_script %>
</p> -->



<%= link_to 'Edit', edit_lesson_path(@lesson) %> |
<%= link_to 'Back', lessons_path %>

<%= link_to 'record prompt', lessons_prompt_view_path %>


<%= link_to 'submit your practice practice', new_lesson_practice_path(@lesson) %>

<%= form_for @lesson, url: explanation_token_lesson_path(@lesson.id),
                                                    html: {id: "tokenForm"},
                                                    remote: true,
                                                    method: 'post' do |f| %>
<%= f.hidden_field :explanation %>
<% end %>

<script >
  // this function is getting the video token from ZiggeoApi.Events
  function postVideoToken(videoToken){
    // grab form by the id been display in the html and store it in a jquery object
    $form = $('#tokenForm');
    // grab the value field by its the id
    $field = $('#coach_field');
    // assign the value of that field we grab by its id to the value of postVideoToken
    $field.val(videoToken);
    // the form we grabed earlier by its id gets submited to the server
    $form.submit();
    // $.ajax ({
    //   url : "/token",
    //   type : "post",
    //   data : {token: videoToken}
    // });
  };
  // function postStreamToken(streamToken){
  //
  // };
      ZiggeoApi.Events.on("submitted", function (data) {

          // getting the string token and storing it in videoToken var to ship it to the outside world
          var videoToken = data.stream.token
          var streamToken = data.stream.video_token
          console.log(videoToken);
          console.log(streamToken);

          // here is where we grab the token to escort it outside of the ziggeo api on click event
          postVideoToken(videoToken);

          // console.log(streamToken);

      });
</script>
