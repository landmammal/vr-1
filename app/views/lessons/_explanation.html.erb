<% if @all_prt || @mdl_prt || @prmt_prt %>
	<% @this_video_token = @prior_expl.video_token %>
	<% @this_added_class = 'explanation' %>
	
    <div class="video_black_bg full">
		<div class="ziggeo_wrapper">
			<div class="ziggeo">
				<%= render './shared/ziggeoplayer' %>
			</div>
		</div>
	</div>

	<div class="lesson_script <%= @this_added_class %>_script">
		<%= simple_format(@prior_expl.script) %>
	</div>
	<%= render 'scriptcontrol' %>
<% end %>