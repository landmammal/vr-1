location.reload(true);
$('.accept_registration').bind('ajax:success', function() {
  $(this).closest('tr').fadeOut();

});

<% flash.each do |type, message| %>
$("#registration_notice").append("<%= j content_tag :p, message.html_safe, class: "flash text-center" %>").delay(3000).fadeOut("slow");
<% end %>