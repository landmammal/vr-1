<li>
  <button class="submission"><%= @rehearsal.id %>
    <script>
      $("ul").on('click',".submission", function () {
        // var example= name {name: name};
        // example.id = '<%= @rehearsal.id %>';
      $.ajax({
        url: '/rehearsals/<%= @rehearsal.id %>/api',
        type: "PUT",
          // data: { @rehearsal = '<%= @rehearsal.id %>' },
        success: function(data){
          console.log(data);
        }

      });

      });
    </script>
  </button>
</li>
