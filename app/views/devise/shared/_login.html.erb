<!-- <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.email_field :email, autofocus: true, placeholder:'Email', class: 'stylize half space-1', value:'aldelcy2000@gmail.com' %>
  </div>

  <div class="field">
    <%= f.password_field :password, autocomplete: "off", placeholder:'Password', class: 'stylize half space-5', value:'550667' %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end -%>

  <div class="actions">
    <%= f.button "Log in", class:'login' %>
  </div>
<% end %>

 -->
