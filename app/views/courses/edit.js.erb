'use strict';

var editCourse = "<%= escape_javascript (render partial: './courses/form',  locals: {course: @course, editMode:true } ) %>";
$('.edit_course').html(editCourse);
$('.edit_course').delay(500).find('.shadebox_bg').addClass('open');
